常见数据类型
    数值
        整数，浮点数，复数，布尔
    字符串
容器数据类型
    列表
    元组
    集合
    字典
特殊数据类型
    None

不可变：数值，字符串，元组
可变：列表，集合，字典

type()不会认为子类是父类
isinstance()会认为子类是父类

小整数池：[-5,256]
大整数池：一开始大整数池为空，每创建一个大整数就往里加一个
注意：大整数池的地址是否相同并不绝对，取决于环境

from decimal import Decimal
浮点数会有精度丢失问题，可以使用Decimal()类

is : 判断是否指向同一个地址

intern 机制
    每个（字符串），不夹杂空格或者特殊符号，默认开启 intern 机制，共享内存，靠引用
    计数决定是否销毁。相同的字符串默认只保留一份，当创建一个字符串，它会先检查内存里
    有没有这个字符串，如果有就不再创建新的了。
字符串缓冲池
    单个字母，长度为 1 的 ASCII 字符会被 interned，包括空字符。

编码和解码

输入
    input()
输出
    print()
格式化输出
    % 占位符
    format()格式化
    f-字符串

运算符
    算术运算符
    赋值运算符
    比较运算符
    逻辑运算符
    位运算符
    成员运算符
    身份运算符

流程控制语句
    顺序
    分支
        单分支
        双分支
        多分支
    循环